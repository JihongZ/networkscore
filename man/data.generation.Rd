% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networkscore.R
\name{data.generation}
\alias{data.generation}
\title{Simulate single and multiple data for factor and network analysis}
\usage{
data.generation(N, J, Lambda, Psi12 = 0, Phi, MisType = NULL)
}
\arguments{
\item{N}{Sample Size.}

\item{J}{Number of items per dimension.}

\item{Lambda}{Matrix of factor loadings with the number of columns same to number of dimensions.}

\item{Psi12}{Residual covariances.}

\item{Phi}{Factor correlation (for multidimensional factor analysis only).}

\item{MisType}{Different types residual covariance matrix (simple, within-factor, and between-factor residual covariances)}
}
\value{
A list including Sample size (N), Number of items (J), Lambda (factor loading matrix), Psi12 (residual covariances), Phi (factor correlation), data (generated data set), true_factor_score (data-generating factor scores).
}
\description{
Simulate single and multiple data for factor and network analysis
}
\examples{
OneRep_onefactor <- data.generation(N = 500, J = 5,
Psi12 = 0.3,
Lambda = matrix(runif(5, .4, .7),5, 1) ,
Phi = matrix(1, 1, 1)) # generate data and true factor scores
str(OneRep_onefactor)
OneRep_twofactor <- data.generation(N = 1000, J = 5,
                                   Psi12 = 0.3,
                                   Lambda = matrix(c(1, runif(4, .4, .7),
                                                     rep(0, 5), rep(0, 5), 1,
                                                     runif(4, .4, .7)), 10, 2),
                                   Phi = matrix(c(1, .3, .3, 1), 2, 2))
str(OneRep_twofactor)
}
